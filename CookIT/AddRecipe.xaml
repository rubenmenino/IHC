<Page x:Class="CookIT.AddRecipe"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CookIT"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddRecipe">

    <Page.Resources>
        <Style TargetType="TextBox" x:Key="RecipeName">
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Foreground" Value="#000"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="MinHeight" Value="20"/>
            <Setter Property="MinWidth" Value="250"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
                            CornerRadius="10"
                            BorderBrush="#bfbfbf"
                            BorderThickness="1"
                            Background="#fff"
                            >
                            <ScrollViewer x:Name="PART_ContentHost"
                            Focusable="false"
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollBarVisibility="Hidden"/>      
                        </Border>
                        
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" x:Key="Description">
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Foreground" Value="#000"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="MinHeight" Value="80"/>
            <Setter Property="MinWidth" Value="250"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
                            CornerRadius="10"
                            BorderBrush="#bfbfbf"
                            BorderThickness="1"
                            Background="#fff"
                            >
                            <ScrollViewer x:Name="PART_ContentHost"
                            Focusable="false"
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollBarVisibility="Hidden"/>
                        </Border>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            
           
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Enter recipe name" Margin="5" Height="30" FontWeight="Bold"/>
        <TextBox Grid.Row="1" Name="RecipeName" Style="{StaticResource RecipeName}" />
        <Label Grid.Row="2" Content="Select the type of food" Margin="5" Height="30" FontWeight="Bold"/>
        <RadioButton Grid.Row="3" Grid.Column="0">Vegan</RadioButton>
        <RadioButton Grid.Row="4" Grid.Column="0">Vegetarian</RadioButton>
        <RadioButton Grid.Row="3" Grid.Column="1">Meet</RadioButton>
        <RadioButton Grid.Row="4" Grid.Column="1">Fast Food</RadioButton>
        <Label Grid.Row="5" Content="Enter recipe description" Margin="5" Height="30" FontWeight="Bold"/>
        <TextBox Grid.Row="6" Grid.RowSpan="4" Grid.Column="0" Grid.ColumnSpan="2" Name="Description" Style="{StaticResource Description}"/>
        <Button Grid.Row="10" Content="OK" Margin="5" />
        
        
    </Grid>
</Page>
